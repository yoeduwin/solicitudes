name: Pruebas backend

on:
  push:
    branches:
      - ex/employee-experience-v1
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Obtener código
        uses: actions/checkout@v4
      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Ejecutar pruebas
        run: node pruebas/correr.js
